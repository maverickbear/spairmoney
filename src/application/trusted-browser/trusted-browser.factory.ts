/**
 * Trusted Browser Factory
 * No injected dependencies; service uses repository and feature-guard directly.
 */

export {
  registerTrustedDevice,
  checkTrustedDevice,
} from "./trusted-browser.service";
