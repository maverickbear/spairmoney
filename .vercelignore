# Docker files (not needed for Vercel builds)
docker-compose.yml
infrastructure/docker-compose.yml

# Local development files
*.local
.env.local

# Documentation at project root (not needed for builds).
# Use /docs/ so we do NOT exclude app/admin/(dashboard)/docs/ (the /admin/docs route).
/docs/

# Scripts (not needed for builds)
scripts/

# Test files
tests/
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx

# Infrastructure files (only root-level infrastructure folder, not src/infrastructure)
/infrastructure/

